# The dsh-git-worktree bundle patch: one insert over the profile root. Later
# layers (the user's profile cordis.patch.yml) may address the row by id.
- insert:
    - id: git-worktree
      name: '@laoyuehanni/dsh-git-worktree'
